# -*- coding: utf-8 -*-
$:.unshift("/Library/RubyMotion/lib")
require 'motion/project'
require 'bundler'
Bundler.require :default, :development

Motion::Project::App.setup do |app|
  app.name = 'define-method'
  app.files += ['lib/module_ext.rb']
end
